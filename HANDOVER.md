# دليل لوحة التحكم — الرواد للتطوير العقاري

> اللوحة على `/admin`. كلها بالعربي ومن اليمين للشمال.
> اللي مكتوب هنا هو كل اللي محتاج تعرفه عشان تشغّل الموقع بنفسك.

---

## ١ — أول تشغيل

```bash
npm install
npm run seed      # مرة واحدة — بيستورد الوحدات والصور والشهادات والمؤشر
```

بعدين افتح ملف **`.env`** واكتب فيه بريدك وكلمة المرور اللي إنت تختارها:

```
ADMIN_EMAIL=admin@alrowadrealestate.com
ADMIN_PASSWORD=اكتب-كلمة-المرور-هنا
```

وشغّل:

```bash
npm run admin     # بيعمل الحساب. ولو الحساب موجود بيغيّر كلمة المرور
npm run dev       # الموقع :3100 واللوحة :3100/admin
```

> **ليه كلمة المرور مش مكتوبة في الكود؟** اللوحة دي هتشيل أسماء وأرقام تليفونات
> عملاء حقيقيين. كلمة مرور مكتوبة في ملف بيترفع على جيت أو في محادثة بتفضل موجودة
> في التاريخ للأبد. ملف `.env` مستثنى من جيت، فكلمة المرور بتفضل عندك إنت بس.
>
> **`npm run admin` هو كمان «نسيت كلمة المرور»** — غيّر السطر في `.env` وشغّله تاني.

⚠️ **البريد لازم يكون بريد حقيقي فيه `@`.** حاجة زي `Admin.com` هتترفض — مفيهاش
علامة `@` ولا نطاق، والسكريبت هيقولك كده بالعربي.

---

## ٢ — اللوحة فيها إيه

| القسم | بيعمل إيه |
|---|---|
| **الوحدات** | كل الوحدات. الإضافة والتعديل والنشر والبيع. |
| **الصور** | مكتبة الصور. ارفع بأعلى جودة والنظام بيقصّها. |
| **آراء العملاء** | الشهادات — بالاسم والمدينة ونوع الوحدة والتاريخ. |
| **مؤشر سعر المتر** | نسخة جديدة كل شهر. القديمة بتفضل منشورة. |
| **الطلبات** | اللي جالك من الموقع. |
| **إعدادات الموقع** | التليفون والعنوان والأرقام الرسمية. |

الصفحة الرئيسية للوحة بتوريك **الشغل المطلوب النهاردة** — مش «أهلاً بيك». لو فيه طلب
مستني رد أو سعر بقاله شهر مامتراجعش، هتلاقيه أول حاجة قدامك.

---

## ٣ — إزاي تضيف وحدة

1. **الوحدات** ← «إنشاء».
2. املأ **الأساسيات**: الكود، الاسم، المنطقة، السعر، المساحة.
3. افتح تبويب **الأوراق** واكتب الحالة القانونية بصدق، ومين راجع الأوراق، وتاريخ الزيارة.
4. افتح تبويب **الصور** وارفع الصور. **حدّد لكل صورة نوعها**:
   - **صورة حقيقية للوحدة** — صورة صوّرتها في المعاينة.
   - **صورة تعبيرية** — صورة مولّدة أو لوحدة مشابهة. الموقع بيكتب اللابل ده للمشتري.
5. غيّر **حالة النشر** إلى «منشورة» ← «حفظ».

### ليه ساعات بيرفض ينشر؟

اللوحة **مش هتنشر وحدة من غير صورة حقيقية واحدة على الأقل**، وهتقولك كده بالعربي.

ده مش تعقيد — ده أهم قاعدة في الموقع كله. المشتري المصري أصلاً بيفترض إن الصور مش
بتاعة الشقة. لو اتمسك مرة واحدة إن صورة مولّدة معروضة على إنها الوحدة، الضرر على السمعة
أكبر بكتير من أي ترتيب في جوجل.

فاعرض الصور المولّدة عادي — بس **مكتوب عليها إنها تعبيرية**.

---

## ٤ — القواعد اللي اللوحة بتفرضها

مش نصايح — دي محطوطة في الكود:

| القاعدة | ليه |
|---|---|
| الحالة القانونية **إجبارية** | ده اللي الموقع كله قايم عليه |
| تاريخ آخر مراجعة للسعر **إجباري** | سعر بلا تاريخ مش سعر |
| تاريخ زيارة الوحدة **إجباري** | منعرضش وحدة مشفناهاش |
| النشر محتاج **صورة حقيقية** | فوق |
| الشهادة محتاجة **اسم ومدينة ونوع وحدة وتاريخ** | شهادة باسم أول ونجمة شكلها مضروب |
| السعر **أرقام بس** بدون فواصل | الموقع بينسّقها صح |
| **مفيش ألوان ولا خطوط** في اللوحة | التصميم محسوم — تغييره بيخرّب التماسك |

⚠️ **متختارش «مسجل بالشهر العقاري» غير لما تشوف رقم التسجيل وتاريخه بعينك.**
الغلطة دي مرة واحدة بتهدّ الموقع كله، لأن المشتري بيسأل على الرقم.

---

## ٥ — الشغل الشهري

**أول كل شهر: نسخة جديدة من مؤشر سعر المتر.**
«مؤشر سعر المتر» ← «إنشاء». **متعدّلش النسخة القديمة** — بتفضل منشورة بتاريخها،
وده اللي بيخلي أرقامك قابلة للمراجعة.

الصفحة دي هي أكتر صفحة هتجيب لك ترتيب في جوجل وأكتر واحدة هيستشهد بيها ChatGPT،
لأنها أرقام مؤرّخة بعيّنة معلومة — وده اللي مفيش حد في السوق بيعمله.

**كل ٣٠ يوم: راجع أسعار الوحدات المنشورة.** اللوحة بتوريك اللي بقاله أكتر من شهر.
وحدة سعرها قديم بتحوّل الموقع من صادق لمضلِّل من غير ما حد يقصد.

---

## ٦ — لما تبيع وحدة

غيّر الحالة إلى **«تم البيع»**، وهيظهر لك تبويب **البيع**. اكتب:
- **سعر البيع الفعلي** — المتعاقد عليه، مش المعلن قبل التفاوض
- **تاريخ إتمام التعاقد**
- **عدد أيام العرض**

الوحدة بتنتقل لسجل البيع العام. **ومبتتشالش منه بعد كده.** السجل ده هو اللي بيخلّي
«٥٠٣ وحدة» رقم يتراجع بدل ما يكون عدّاد على الشاشة.

---

## ٧ — قبل ما الموقع ينزل

الأرقام دي كلها دلوقتي **من التصميم مش حقيقية**. لازم تتغير:

- [ ] **السجل التجاري والبطاقة الضريبية ورقم تسجيل الوساطة** (إعدادات الموقع ← الشرعية القانونية)
- [ ] كل أسعار الوحدات وأكوادها
- [ ] سجل البيع كله
- [ ] أرقام العائد في صفحة الخليج
- [ ] الشهادات — بأسماء حقيقية أو تتشال

⚠️ **رقم تسجيل الوساطة العقارية:** لازم تكون مسجّل فعلاً في GOEIC تحت القرار الوزاري
٥٧٨/٢٠٢٥ قبل ما تنشر رقم. القطاع اتنظّم من يناير ٢٠٢٦، ونشر رقم مش عندك بيفتح باب
لشكوى من منافس مسجّل.

---

## ٨ — النشر: Neon + Vercel

**Neon هو الاختيار الصح** — Postgres سيرفرلس، فيه طبقة مجانية تكفي الموقع ده براحة،
وبيقفل نفسه لما مفيش استخدام فمش بتدفع في الفاضي.

اللوحة دلوقتي على **SQLite** (ملف `alrowad.db`) — كويس على اللابتوب، بس **مينفعش
على Vercel**: السيرفرلس مفيهوش قرص دائم، فالملف بيتمسح مع كل نشر.

**الكود بيبدّل لوحده.** مفيش أي تعديل مطلوب وقت النشر — بيقرأ أول الرابط:

| `DATABASE_URI` | بيستخدم |
|---|---|
| `file:./alrowad.db` | SQLite |
| `postgresql://…` | Postgres |

### خطوات Neon

1. اعمل حساب على **neon.tech** ← New Project.
2. **الريجن: `eu-central-1` (فرانكفورت).** دي أقرب حاجة لمصر — الترافيك المصري
   بيعدّي على أوروبا، فواشنطن بتضيف حوالي ١٥٠ مللي ثانية على كل طلب.
3. من الـDashboard: **Connection string ← Pooled connection**.
   خد اللي فيه `-pooler` في العنوان — من غيره السيرفرلس بيفتح اتصالات أكتر من
   اللازم وبتقع.
4. حط الرابط في `.env` محليًا للتجربة، وفي **Vercel ← Settings ← Environment
   Variables** للنشر.
5. أول مرة تشغّل، Payload بيعمل الجداول لوحده. بعدين:
   ```bash
   npm run seed
   npm run admin
   ```

### باقي النشر

- **`PAYLOAD_SECRET`** — ولّده بـ `openssl rand -base64 32`. متسيبوش على القيمة
  الافتراضية.
- **الصور** دلوقتي بتتخزن في `public/uploads`، وده كمان بيتمسح على Vercel.
  محتاج **Cloudflare R2** عن طريق `@payloadcms/storage-s3`.
- **`preferredRegion = 'fra1'`** متحطوط أصلاً في `next.config.ts`.
- **الدومين:** A و CNAME عند Namecheap (مش nameservers — عشان تحتفظ بتحويل
  الإيميل المجاني). امسح الـ`parkingpage` records الافتراضية.
  **متشتريش شهادة SSL** — Vercel بيعملها ببلاش وبيجددها لوحدها.
