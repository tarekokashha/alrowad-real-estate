# dependencies
node_modules/

# next
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env — never commit; contains DB URL, secret and admin password
.env*
!.env.example

# payload
alrowad.db
alrowad.db-*
public/uploads/
# payload-types.ts is a BUILD INPUT, not an artefact: components/admin/Welcome.tsx
# imports it, so a clean checkout without it fails `next build` with TS2307.
# Regenerate with `npm run generate:types` after changing any collection.

# os
.DS_Store
Thumbs.db
